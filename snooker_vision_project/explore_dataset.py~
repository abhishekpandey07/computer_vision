import numpy as np
import cv2
from os import path
from glob import glob

captures = 'dataset/captures'
files  = glob(captures+'/*.tiff')

img=cv2.imread(files[4])
gray = cv2.cvtColor(img,cv2.COLOR_BGR2GRAY)
cv2.imshow(files[4],img)
cv2.waitKey(0)

'''img_equi_hist = cv2.equalizeHist(gray)
cv2.imshow('img_equi_hist', img_equi_hist)
cv2.waitKey(0)'''

img_canny =  cv2.Canny(blur,25,200)
cv2.imshow('img_canny',img_canny)
cv2.waitKey(0)
